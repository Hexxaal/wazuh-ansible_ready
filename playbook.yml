---
- name: Deploy Wazuh Docker single node with Cloudflare certs
  hosts: localhost
  connection: local
  gather_facts: false

  collections:
    - community.docker

  roles:
    - clone_wazuh_docker
    - configure_cert        # now generates & injects certs
    - deploy
